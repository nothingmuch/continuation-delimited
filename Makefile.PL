use 5.010000;
use ExtUtils::MakeMaker;

WriteMakefile(
    NAME              => 'Continuation::Delimited',
    VERSION_FROM      => 'lib/Continuation/Delimited.pm',
    PREREQ_PM         => {},
	SIGN              => 1,
);
